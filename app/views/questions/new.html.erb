<h1>New Question</h1>

<div class="row">
  <div class="col-md-4">
    <p>Insert a new question rules are...</p>
    <ol>
      <li>Don't ask something you already know.</li>
      <li>Don't mock others with your questions.</li>
      <li>Don't be tepid ask.</li>
    </ol>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |q| %>
      <div class="form-group">
        <%= q.label :title %>
        <%= q.text_field :title, class: 'form-control', placeholder: 'Enter the question title' %>
      </div>
      <div class="form-group">
        <%= q.label :body %>
        <%= q.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter the question contents" %>
      </div>
      <div class="form-group">
        <%= q.label :resolved, class: 'checkbox' do %>
          <%= q.check_box :resolved %> resolved
        <% end %>
      </div>
      <%= q.submit "Save", class: 'btn btn-success' %>
      <% end %>
    </div>
  </div>
