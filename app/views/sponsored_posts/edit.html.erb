<h1>Edit Sponsored Post</h1>
<div class="row">
  <div class="col-md-4">
    <p>Guidelines</p>
    <ul>
      <li>Endorse.</li>
      <li>Endorse.</li>
      <li>Endorse.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@sponsoredpost.topic, @sponsoredpost] do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter the sponsored post title." %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, class: "form-control", placeholder: "Enter the sponsorship." %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.number_field :price, class: "form-control", placeholder: "Enter the price." %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: "btn btn-success" %>
      </div>
    <% end %>
  </div>
</div>
